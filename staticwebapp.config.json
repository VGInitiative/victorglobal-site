{
  "routes": [
    {
      "route": "/admin.html",
      "allowedRoles": ["authenticated", "administrator"]
    },
    {
      "route": "/api/*",
      "allowedRoles": ["authenticated", "administrator"]
    }
  ]
}